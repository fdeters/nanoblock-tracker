name: Monthly Nanoblock Sync

on:
  schedule:
    - cron: "0 9 1 * *"
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        id: run_scraper
        env:
          GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          GOOGLE_SHEET_NAME: ${{ vars.GOOGLE_SHEET_NAME }}
        run: |
          set -euo pipefail
          if [ -z "$GOOGLE_SHEET_ID" ]; then
            echo "Google Sheets sync failed: GOOGLE_SHEET_ID is not configured"
            exit 1
          fi
          if [ -z "$GOOGLE_CREDENTIALS_JSON" ]; then
            echo "Google Sheets sync failed: GOOGLE_CREDENTIALS_JSON is not configured"
            exit 1
          fi

          GOOGLE_SHEET_NAME="${GOOGLE_SHEET_NAME:-Sheet1}"
          python nanoblock_scraper.py --sheet-id "$GOOGLE_SHEET_ID" --credentials "$GOOGLE_CREDENTIALS_JSON" --sheet-name "$GOOGLE_SHEET_NAME" 2>&1 | tee sync-output.txt

      - name: Publish sync summary
        if: always()
        run: |
          {
            if [ "${{ steps.run_scraper.outcome }}" = "failure" ]; then
              echo "## Nanoblock sync summary — Run failed"
            else
              echo "## Nanoblock sync summary"
            fi
            echo
            if [ -s sync-output.txt ]; then
              cat sync-output.txt
            else
              echo "_No output was captured._"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
